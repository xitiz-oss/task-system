import React from 'react';

const DashboardPage = () => {
  return (
    <div>
      <h1 className="text-2xl font-bold mb-4">Dashboard</h1>
      <p>Summary of all tasks categorized by status (e.g., To Do, In Progress, Completed).</p>
      {/* Add components to display task counts, upcoming deadlines, etc. */}
    </div>
  );
};

export default DashboardPage;
